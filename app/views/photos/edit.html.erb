<% 
  if user_signed_in?
%>
<h1>Editing Photo</h1>

<%= render 'form', photo: @photo %>

<%= link_to 'Show', @photo %> |
<%= link_to 'Back', photos_path %>
<% else 
  controller.redirect_to "/users/sign_in"
end %>
